/*
#include "ComplexContainer.h"

ComplexContainer :: ComplexContainer()
{
    this->elems =
}

ComplexContainer :: ComplexContainer(Complex elems[])
{
    this->elems = elems;
}

void ComplexContainer :: sort_me()
{
    int l = sizeof(elems) / sizeof(elems[0]);
    for(int i = 0; i < l - 1; i++)
        for (int j = i + 1; j < l; j++)
            if (elems[j] < elems[i]) swap ( elems[j], elems[i] );
}
*/
